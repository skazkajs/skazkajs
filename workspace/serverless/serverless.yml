service: skazka-serverless-example

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: us-east-1
  environment:
    NODE_ENV: production

functions:
  app:
    handler: index.handler
    events:
      - httpApi: '*'
